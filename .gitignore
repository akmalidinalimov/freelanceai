# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# playwright e2e
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local
.env.local
.env.development
.env.production

# prisma
/prisma/*.db
/prisma/*.db-journal

# typescript / build info
*.tsbuildinfo

# testing
/coverage

# vercel
.vercel

# IDE
.idea/
.vscode/*
!.vscode/extensions.json

# Local MCP config (contains the Hostinger API token — never commit)
/.mcp.json

# Local-only design mockups / labs (kept on disk to revisit, never committed —
# the shipped design system lives in the code + DESIGN.md/PRODUCT.md)
/design-*.html
/design-pages/
/design-assets/
/.impeccable/
