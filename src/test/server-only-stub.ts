// Test stub: `server-only` throws outside a React Server Component bundler.
// Vitest aliases the package to this empty module so server modules that import
// it (for safety in the app) remain unit-testable.
export {};
